extends base

block content
  .div-cours
    p.cours courses:
  .courses
    each c in courses
      button.btn-clases(data-courseid= `${c._id}`)
        .first-cours
          .class-img-container
            img.first-cours-img(src=`/imgs/courses/${c.photoUrl}`, alt=`course card`)
          .class-info 
            .asab= c.title
            .about-cours= c.description
            .price-container
              .price= c.price
              buttton.buy شراء الكورس
  button.upload-btn +
  
block scripts
  script(src='/js/courses.js')