doctype html
html(lang='ar')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(http-equiv='Content-Type', content='text/html;charset=UTF-8')
    link(rel='stylesheet', href='/css/uploadCourse.css')
    title Document
  body
    .c
      .correct.hidden
        .correct-message you buy it 
    .center
      .header
        .upload-class تعديل الكورس 
      .container
        .form
          .label-input
            label(for='class-name', dir='rtl', lang='ar') &#x627;&#x633;&#x645; &#x627;&#x644;&#x643;&#x648;&#x631;&#x633; 
          input#subcourse-name.input(type='text', value= `${cToEdit.title ? cToEdit.title : ' '}`)
          .label-input
            label.label(for='class-des', dir='rtl', lang='ar') &#x627;&#x644;&#x648;&#x635;&#x641;
          - const des = cToEdit.description ? cToEdit.description : ' '
          input#subcourse-des.input(type='text', value= `${des}`)
          .label-input
            label(for='image', dir='rtl', lang='ar') &#x627;&#x644;&#x635;&#x648;&#x631;&#x629;
          input#image.input(type='text', value= `${cToEdit.photoUrl ? cToEdit.photoUrl : ' '}`)
          .label-input
            label(for='price', dir='rtl', lang='ar') &#x627;&#x644;&#x633;&#x639;&#x631;
          input#price.input(type='number', dir='rtl', lang='en', value= `${cToEdit.price ? cToEdit.price : ' '}`)
        .btn
          button.btn-sub(dir='rtl', lang='ar') تعديل
    script(defer, src='/js/editSubcourse.js')